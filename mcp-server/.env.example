# Alchemy API Key for standard networks
NEXT_PUBLIC_ALCHEMY_API_KEY="your-alchemy-api-key"

# RPC URL for Arc Testnet
RPC_ARC_TESTNET="https://rpc.testnet.arc.network"

# Optional: Port for the MCP Server if running over HTTP
PORT=3000
